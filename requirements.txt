# Core DL (course framework)
# Pinned exactly: the model is saved AND loaded under this version. A loose
# range let Colab (2.20.0) drift from installs that resolved <2.20, producing a
# BatchNorm `renorm` config mismatch that crashed the eval notebook on load.
tensorflow==2.20.0

# Detection + tracking (CV course labs)
ultralytics>=8.2

# Image processing
opencv-python>=4.9
pillow
numpy
matplotlib

# Metrics / data / eval (notebook 02b) + forecasting (notebook 03)
scikit-learn
pandas
seaborn   # confusion-matrix heatmap in evaluation
joblib    # persisted MinMaxScaler for the LSTM forecast (backend/forecast.py)

# Backend
fastapi
uvicorn[standard]
python-multipart

# Notebooks
jupyter
kaggle  # dataset download (needs ~/.kaggle/kaggle.json)
